---
applyTo: '**'
---

<todos title="Reflex dashboard redesign" agentRequirement="Review steps frequently throughout the conversation and DO NOT stop between steps unless they explicitly require it. Keep the todos updated using the todo_write tool (do not edit this file).">
- [x] model-agent-sessions: Refactor app.py state to use an AgentSession BaseModel list with logs, busy state, and tier_status plus global command inputs. 🔴
  _Replaced the older dict-based dashboard state with a Pydantic AgentSession model and a simulation-first global command flow to match the requested UI contract._
- [x] rebuild-dashboard-layout: Implement the left sidebar, 2x2 session grid rendered with rx.foreach, terminal log panes, and persistent bottom command bar with dark neon styling. 🔴
  _Rebuilt the dashboard shell around a left health sidebar, 2x2 session matrix, cyan audit panels, and a persistent command bar rather than layering more state onto the older per-panel prompt UI._
- [x] validate-reflex-compatibility: Run syntax and import validation on the redesigned Reflex app and fix any issues introduced by the rewrite. 🔴
  _Validated with editor diagnostics plus py_compile/import smoke, then resolved the event-handler typing issues and replaced deprecated rx.Base usage with a standard Pydantic BaseModel._
</todos>

<!-- Auto-generated todo section -->
<!-- Add your custom Copilot instructions below -->
